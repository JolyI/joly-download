# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`。
每个版本都在 git 里打了 tag，可以直接 `git checkout <tag>` 回到那一版。

---

## v1.4.0 · 2026-09-24 · 清爽界面与文件图标

面板与设置页改为灰白柔蓝主题，统一文件图标与扩展品牌图标。
沿用现有下载管理功能、权限和列表分组规则，无新增运行时依赖或定时器。

- **清爽布局**：移除国潮纹样、粗描边与硬阴影，文件名与状态、来源与大小时间各占一行，
  当前任务只用浅底色和细边框突出，保持列表信息层级一致。
- **横向进度条**：恢复细横条，与百分比共用 `--p`，通过 CSS 伪元素绘制；
  空状态改为下载线框图标，移除完成盖章动效，并保留减少动态效果支持。
- **18 类文件图标**：统一为浅色圆角底与居中符号，区分 PDF、Word、表格、演示文稿、
  纯文本、Markdown、代码、图片、矢量图、视频、音频、压缩包、磁盘镜像、安装包、
  字体、设计文件、电子书及其他文件；亮暗主题分别调整配色。
- **图标识别**：按扩展名匹配，支持大小写与通用回退；图标细分独立于原有七种分组，
  同组改名可以更新图标，进度更新复用已有节点。
- **扩展图标**：使用透明底的 J 字母与下载箭头组合，更新 16 / 32 / 48 / 128 像素资源。
- **设置页**：同步主题、间距、边框与焦点样式，适配窄窗口。
- **开发与说明**：更新面板预览，新增文件图标预览与 `node tools/check-file-icons.cjs` 检查，
  补充版本安装包、安装与升级说明。

继续遵循低内存约定：无 content script、无空闲轮询、按需刷新、增量渲染与列表条数上限。
内存阈值仍是验收目标，本次未重新实测进程内存；静态预览和图标检查不覆盖完整扩展交互。

## v1.3.0 · 2026-09-21 ·「国潮」

**UI 第四次改版。功能与 v1.2.0 完全一致。**

v1.1 刷颜色、v1.2 刷纹样，但骨架始终是「标题栏 + 标签栏 + 搜索栏 + 等权重竖列表」，
所以看着还是旧。v1.3 换的是**构图与层级**。

改动：

- **顶栏**：中国红实块 + 同心圆藻井，标题 900 字重 + 硬偏移阴影
- **可点元素统一成「贴纸」**：白底 + 2px 墨描边 + 2px 硬偏移阴影，按下时位移贴合阴影
- **主角卡**：正在下载的那一件抬成大卡（大佛龛、30px 百分比、「下载中」角标），
  其余下载压成单行贴纸
- **榜题**：墨底金字标牌 + 红底件数印
- **佛龛**：拱顶小龛，金箔从龛底涌起表达进度
- 深色模式重做；榜题在深色下**反转成金底墨字**
- 设置页同步国潮语言

### 主角卡的实现代价是零

主角卡**就是列表里的第一行，只多一个 `.is-hero` 类**。`pickHero()` 挑出第一条
还在跑的下载，`renderView()` 顺手 toggle。**DOM 一个节点都没多**，更新路径与
普通行完全一致 —— 没有新增定时器，没有新增内存驻留。

同一个 DOM 呈现两副样子，靠 `display: contents` 把 `l1 / l2 / bar-row` 摊平进
`.main` 再用 `order` 重排；`.is-hero` 下还原回块级。

新增 `tools/preview-idle.html`（没有下载时主角卡退场）。三个预览页都严格照
`createRow()` 的 DOM 层级重写，否则测不到 `display: contents` 的摊平逻辑。

## v1.2.0 · 2026-09-21 ·「敦煌 · 石窟」

**UI 第三次改版。功能与 v1.1.0 完全一致。**

v1.1 只换了颜色，骨架还是通用列表 —— 所以"有敦煌味，但不像敦煌"。
v1.2 换的是**构图语法**：敦煌壁画怎么组织一面墙，面板就怎么组织。

| 壁画的构件 | 界面里对应什么 |
|---|---|
| 藻井（窟顶中心） | 顶栏：放大的藻井团花虚压中轴，标题与按钮完全轴对称 |
| 佛龛（拱顶小龛） | 每行文件图标的容器 —— 拱形龛，不再是圆角方块 |
| 贴金（给造像上金箔） | 进度：金箔从龛底涌起，涌到的高度就是进度，镀满即完成 |
| 榜题（每铺画的标牌） | 分组头：土红底 + 金边，件数是一枚小金印 |
| 木签 | 筛选标签 |

改动：

- **顶栏重做**：从一条扁平标题栏改成藻井华盖 —— 藻井团花虚压中轴、标题居中、
  操作按钮另起一行居中，完全轴对称；底边保留金飘带界栏
- **进度条删除**：改由佛龛贴金表达。进度写进行节点的 `--p`，由 `.ic-box::before`
  的 `scaleY` 驱动，纯 CSS，无额外 DOM、无额外定时器
- **文件图标改拱形佛龛**：`border-radius: 16px 16px 5px 5px`
- **分组头改榜题**：土红底 + 金边标牌，件数改金色小印
- **筛选标签改木签**：方角、有底，选中时底边压一道金
- **扩展图标重做**：土红底 + 两道金线方胜箍 + 正中向下的箭（原来是通用蓝箭头）
- 设置页顶部换成同一枚藻井标记

### v1.1.0 · 2026-09-21 ·「敦煌」

**UI 第二次改版。功能与 v1.0.0 完全一致，只换皮。**

主题：莫高窟壁画。暖砂壁面 + 土红藻井带 + 石青/石绿/铅丹三种矿彩 + 金线。

| 色 | 用在哪 |
|---|---|
| 石青 | 进行中、选中标签、焦点环、进度条 |
| 石绿 | 已完成 |
| 铅丹（土红） | 失败、删除、分组计数印、危险开关 |
| 金 | 纯装饰：藻井菱格、飘带界栏、标题、飞天 |
| 土红 | 顶部藻井带 |

细节：

- 顶部藻井带：土红底 + 金线菱格（SVG data-URI 平铺），底边压一道金飘带界栏
- 标题前的「载」由朱砂印改为**金印**
- 空状态由印章改为**飞天**（`currentColor` 描线，配色交给 CSS）
- 进度条由「靛青→朱砂」改为**石青 → 金**
- 壁面加了**斑驳**：两层大块褪色 + 两层细颗粒，纯静态背景
- 深色模式改为「洞窟内」配色：壁面变深、矿彩提亮
- 开关的"危险"态单独用一个更亮的橙红 —— 土红顶栏上铅丹不够跳
- 设置页同步同一套语言（顶部拉通一条藻井带）

新增 `tools/preview.html` / `tools/preview-empty.html`：离线静态预览，引用真
`popup.css`，改 UI 时用 Chrome 无头截图即可，不必反复重载扩展。
`docs/panel.png` 改为由该预览渲染生成。

## v1.0.0 · 2026-09-21 ·「朱墨」

**UI 首次改版。功能与 v0.5.1 完全一致，只换皮。**

主题：宣纸底 + 墨字 + 靛青点睛 + 朱砂印章。
两组撞色**分工严格分离**，所以敢撞而不会撞出歧义：

| 色 | 管什么 |
|---|---|
| **靛青** | 进行中、选中标签、焦点环、进度条 —— "活着的东西" |
| **朱砂** | 印章、界栏、失败、删除、危险开关 —— "印章与危险" |
| **石绿 / 藤黄** | 已完成 / 已暂停 · 收尾中 |

细节：

- 宣纸纤维背景：两层错位点阵，纯静态，不参与合成也不重绘
- 标题与分组头用**楷体**；正文仍是系统无衬线（小字号下楷体太糊）
- 界栏：标题栏底边一道**朱砂水波纹**（SVG data-URI，repeat-x）
- 印章系统：标题前「载」印、空状态「空」印、分组头朱砂计数小印、版本号朱砂款识
- **完成盖章**：下载刚完成时图标底衬盖一次章。只在状态【由非完成转为完成】时播放，
  首次渲染已完成的旧记录不会满屏盖章
- 进度条本身是一次撞色：靛青打底，收尾处转朱砂
- 行左侧状态色条；**已完成默认隐藏**，避免整列泛绿
- 深色模式是「墨底 + 米白」的独立配色，不是简单反色
- 设置页同步同一套语言

性能约定未让步：零依赖、零 content script、零空闲定时器；
新增装饰全部走 CSS 伪元素，**没有新增任何 DOM 节点**。

## v0.5.1 · 2026-09-21

- **修复「打开文件」报错** `The "downloads.open" permission is required`：
  `chrome.downloads.open()` 需要**独立的** `downloads.open` 权限，它不在 `downloads` 之内，必须单独声明
- 新增顶部「**删除含文件**」开关：决定点垃圾桶时是否连磁盘文件一起删。
  开启时开关变红、垃圾桶换带 × 图标并常驻红色；「清空」按钮不受影响，永远只删记录
- 新增「永久删除文件」（`removeFile` + `erase`），不进废纸篓

## v0.5.0 · 2026-09-21

- **移除「接管原生下载界面」开关**。它会让 Safe Browsing 拦截的下载**静默卡在 100%**：
  关掉原生 UI 后 Chrome 无处弹出「保留」确认框，且不会给该下载打危险标记，
  客户端连 `acceptDanger` 都无从下手
- 附带一次性迁移：启动时读旧开关，若曾开启则自动复原原生下载界面
- `downloads.ui` 权限仅为这段迁移保留

## v0.4.0

- 面板形态从侧边栏改为**浏览器动作弹出窗（popup）**，关闭即销毁、内存归零
- 打开性能：首屏分片渲染 + 操作按钮懒创建 + 设置读取不阻塞数据查询
- **100% 卡死治理**：区分两种成因，分别给出「保留」（`acceptDanger`）与「救活」（`pause → resume`）
- 修复进行中行出现 `NaN%`（`receivedBytes` 缺失时退回磁盘文件大小）
- 工具栏角标改为**红色数字**，事件驱动、空闲零开销
