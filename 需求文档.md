# Joly 下载 —— 需求文档 v1.5

> **一句话定位：接管 Chrome 下载，用一块好看的弹出面板，替代 chrome://downloads 页面。**

- 目标用户：仅本人使用（开发者模式 load unpacked 加载，不上架商店）
- 平台：仅 Chrome（本机 Chrome 153.0.8010.53，MV3）
- 面板形态：**浏览器动作弹出窗 popup**（点插件图标弹出，380 x 560；点击页面其它位置自动关闭，关闭即销毁、内存归零）
  - 变更记录：v1.1~v1.3 为 Side Panel 侧边栏，v1.4 按用户要求改为 popup。代价是无法长期挂起看进度，收益是内存更省、不占屏幕。
- ⚠️ **硬约束：极低内存占用（见 §4，为最高优先级非功能需求）**

---

## 1. 要解决的痛点（来自真实使用反馈）

| # | 痛点 | 对应解法 |
|---|---|---|
| 1 | 查看下载要专门打开 chrome://downloads 页面，麻烦 | 弹出面板，点图标即见，一步查看与操作 |
| 2 | 现有插件（下载管理 2.1.0）**面板不好看** | 自研 UI：跟随系统深浅色 + 紧凑列表，现代桌面应用观感 |
| 3 | 现有插件**不能一键打开所在文件夹** | chrome.downloads.show(id)，一个 API 调用在 Finder 中定位文件 |

> 说明：痛点 3 技术上毫无障碍，属于现成插件"没做这个按钮"。本项目 80% 的工作量在 **UI 与交互打磨**上。

---

## 2. 功能范围（用户已确认：全选）

### 2.1 弹出面板（核心载体）
- 点插件图标 → 弹出固定尺寸面板（380 x 560）；
- 面板打开期间持续反映下载状态（事件驱动 + 有活跃下载时的 1s 兜底）；
- 点击页面其它位置即关闭；关闭时页面销毁，所有定时器随之释放。

### 2.2 ~~完全接管浏览器下载~~（v1.5 已移除）
❌ 曾用 chrome.downloads.setUiOptions({ enabled: false }) 隐藏 Chrome 原生下载 UI。

**实测结论（同一文件 A/B 对照）**：
- 关闭接管 → Chrome 弹出安全下载的「保留」确认框 → 点击即完成；
- 开启接管 → 无 UI 可弹 → **永久静默卡在 100%**：不报错、不完成，
  且 Chrome 不会给该下载打危险标记（danger 仍为 safe），
  导致 acceptDanger 也无法代用户确认——**客户端完全无解**。

结论：该功能与"下载能正常完成"不可兼得，予以移除。
SW 启动时会读一次旧开关，若曾开启则自动复原原生下载界面。

### 2.3 下载条目操作（8 项，全部要做）

| 操作 | 实现 API | 说明 |
|---|---|---|
| ⭐ 打开所在文件夹 | chrome.downloads.show(id) | 在 Finder 中定位并选中该文件 |
| 打开文件 | chrome.downloads.open(id) | 用系统默认应用打开 |
| 暂停 / 继续 | pause(id) / resume(id) | Chrome 原生支持断点，任意状态切换 |
| 取消下载 | cancel(id) | 中止进行中的任务 |
| 重试失败项 | download({ url }) | 用原 URL 重新发起 |
| 复制下载链接 | navigator.clipboard | 复制原始 URL |
| 删除记录 | erase({ id }) | **仅移除列表记录，磁盘文件原封不动** |
| 删除（含文件） | removeFile(id) + erase({ id }) | 由面板顶部「删除含文件」开关控制；开启时删除磁盘文件并移除记录，不进废纸篓、不可恢复 |
| 清空已完成 | erase({ state: 'complete' }) | 一键清空历史（同样不动文件） |

### 2.4 列表能力（6 项，全部要做）
- **搜索框**：按文件名 / 来源站点实时过滤；
- **状态筛选**：全部 / 进行中 / 已完成 / 失败 标签页切换；
- **按日期分组**：今天 / 昨天 / 更早；
- **实时速度与进度**：进度条 + 百分比 + 下载速度 + 剩余时间；
- **按类型分类**：图片 / 视频 / 文档 / 压缩包 / 安装包 / 其他（按扩展名判定）；
- **来源站点显示**：展示文件来自哪个网站（从 url 解析域名）。

### 2.5 视觉风格
- 跟随系统深色 / 浅色（prefers-color-scheme）；
- 紧凑列表布局，参考 macOS 访达 / 现代桌面应用；
- 文件类型图标、状态色（进行中=蓝 / 完成=绿 / 失败=红）；
- 悬浮显示操作按钮，保持列表清爽。

---

## 3. 技术方案

### 3.1 架构（事件驱动，面板关闭即零开销）

    ┌──────────────────────────────────────────────┐
    │  弹出窗页面 (popup.html/js)                    │
    │  · 打开时拉一次列表（limit 200）               │
    │  · 注册 downloads 事件做【增量 DOM 更新】       │
    │  · 有活跃下载时才开启兜底定时器，空闲即清除      │
    │  · 面板关闭 → 页面销毁 → 内存归零               │
    └───────────────┬──────────────────────────────┘
                    │ chrome.downloads.*
    ┌───────────────▼──────────────────────────────┐
    │  Service Worker (background.js) —— 极简无状态  │
    │  · 启动时隐藏原生下载 UI（只跑一次）            │
    │  · 监听 onCreated/onChanged/onErased → 角标     │
    │  · 完成/失败通知                               │
    │  · 无定时器、无缓存、无常驻数据 → MV3 自动休眠   │
    └──────────────────────────────────────────────┘

    【无 content script】不向任何网页注入代码。

### 3.2 关键架构决策

| 决策 | 理由 |
|---|---|
| **零 content script** | 不向任何标签页注入代码。若注入 all_urls，每个打开的标签页都要承担一份脚本实例内存——这是扩展内存占用的头号来源，本项目**完全不需要**它 |
| **事件驱动替代高频轮询** | 面板页面自己监听 chrome.downloads.onChanged 做增量更新，而非定时全量 search |
| **只查询需要的数据** | 历史列表一次 search({ limit: 200, orderBy: ['-startTime'] })；活跃任务只查 state: 'in_progress'（结果集极小） |
| **增量 DOM 更新** | 进度变化只改那一行的进度条宽度与文字，绝不整表重渲染 |
| **条件化定时器** | 仅在存在进行中任务时才有兜底定时器（1s）；全部结束立即 clearInterval。空闲时**零定时器、零 CPU** |
| **纯原生 JS，零框架** | 不引入 React/Vue，省掉框架运行时与虚拟 DOM 的内存开销 |
| **列表硬上限 + 滚动加载** | 最多同时渲染 200 行，更早的历史按需加载 |
| **本地 SVG 类型图标** | 不预加载 favicon / 缩略图，避免图片解码缓存占用 |
| **不镜像历史到 storage** | storage 只存设置项（几百字节），不复制下载记录 |
| **速度自己算** | API 不提供速度字段，用 (receivedBytes, timestamp) 差分，只保留最近一次采样 |

### 3.3 需要的权限

    "permissions": ["downloads", "downloads.open", "downloads.ui", "storage", "notifications"]

- **downloads.open**：`chrome.downloads.open()` 的**独立前置权限**。它不在 downloads 之内，必须单独声明，
  缺了会在点「打开文件」时报 `The "downloads.open" permission is required`；
- **downloads.ui**：**仅为一次性迁移保留**（复原旧版本隐藏掉的原生下载界面），无其它用途，迁移完成后可移除；
- **不需要 content_scripts 字段**，也不需要 host_permissions；
- 下载 API 不要求站点权限，权限面很干净；
- 纯本地运行，不联网、不上传任何数据。

### 3.4 主要用到的 API
- **chrome.action**：default_popup 声明弹出窗（无需 SW 注册）；setBadgeText 显示角标
- **chrome.downloads**：search / show / open / pause / resume / cancel / erase / removeFile / download / acceptDanger / onCreated / onChanged / onErased
  —— removeFile **只用于用户显式二次确认的「永久删除文件」**；其余删除类操作只作用于下载历史，绝不动磁盘文件
- **chrome.action**：setBadgeText / setBadgeBackgroundColor
- **chrome.notifications**：下载完成 / 失败提醒

---

## 4. 性能与内存要求（硬约束，最高优先级）

> 用户明确要求：**一定一定要低内存占用**。以下指标为验收红线，任何功能不得以突破预算为代价。

### 4.1 内存预算

| 场景 | 目标上限 |
|---|---|
| 面板关闭（绝大多数时间） | **≈ 0**：渲染进程不驻留；SW 空闲休眠；无定时器 |
| 面板打开，无任何下载任务（空闲） | < 15 MB |
| 面板打开，10 个进行中 + 200 条历史 | < 30 MB |
| 空闲时 CPU 占用 | 0%（无定时器、无轮询、无动画） |
| 空闲时网络请求 | 0 |

### 4.2 明确禁止的实现方式（红线）

- ❌ 常驻 setInterval 轮询（必须条件化、可清除）
- ❌ 引入 React / Vue / jQuery 等任何框架或大型依赖
- ❌ 向所有页面注入 content script
- ❌ 把下载历史镜像写入 chrome.storage
- ❌ 列表整表 innerHTML 重渲染
- ❌ 预加载 favicon / 缩略图（用本地 SVG 图标）
- ❌ 在 Service Worker 中保存常驻状态或缓存

### 4.3 验收方法

1. Chrome 菜单 → 更多工具 → **任务管理器**（Shift+Esc），观察扩展进程内存；
2. 分别在"面板关闭 / 面板打开空闲 / 面板打开且有下载"三种状态下记录数值；
3. 打开面板静置 1 分钟，确认内存无持续增长（无泄漏）。

---

## 5. 明确不做

- ❌ 修改或替换 chrome://downloads 页面本身（无法覆盖，也不需要）
- ❌ 视频流嗅探下载（版权风险）
- ❌ 下载加速 / 多线程 / 限速（Chrome 原生不支持，且属于另一类产品）
- ❌ 跨浏览器兼容（Firefox / Safari）
- ❌ 上架 Chrome 商店、云同步、多设备
- ❌ 下载内容预览、内置播放器（会显著推高内存）

---

## 6. 里程碑（先能用、再好用、再打磨）

### M1 —— 能替代现有插件（最小可用）✅ 已完成
- [x] MV3 骨架：manifest（无 content_scripts）+ 弹出窗声明
- [x] 弹出窗列表渲染（一次 search，limit 200）
- [x] 条目操作：打开所在文件夹 ⭐、打开文件、删除记录、清空已完成
- [x] 跟随系统深浅色、纯原生 JS、零依赖
- **验收**：能卸载"下载管理 2.1.0"，日常查看下载 / 定位文件完全够用；**空闲内存 < 15 MB**

### M2 —— 功能完整 ✅ 已完成
- [x] 实时进度条 + 速度（EMA 平滑）+ 剩余时间（事件驱动 + 条件化 1s 兜底）
- [x] 暂停 / 继续 / 取消 / 重新下载 / 复制链接
- [x] 搜索框、状态筛选标签、日期分组、类型分组、来源站点
- [x] 设置页（分组方式 / 列表条数上限）
- **验收**：功能不输主流下载管理插件；**200 条列表下内存 < 30 MB，无泄漏**

### M3 —— 打磨与卡死治理 ✅ 已完成
- [x] ~~完全接管：隐藏原生下载 UI~~ → **v1.5 移除**（见 §2.2，与"下载能正常完成"不可兼得）
- [x] 角标显示进行中数量（红色数字；事件驱动，进度事件不触发查询；空闲零开销）
- [x] 完成 / 失败系统通知（800ms 批处理聚合防刷屏；点击完成通知 → 访达定位文件；主动取消不通知）
- [x] 设置页（分组方式 / 列表条数 / 完成通知 / 失败通知）
- [x] **100% 卡死治理**：区分两种成因并各给按钮
  - 「已拦截」（danger 非 safe）→ 盾牌按钮执行「保留」(acceptDanger)
  - 「收尾中…」（数据已收齐但状态不翻转）→「救活」按钮 pause→resume 断开重连
  - 两种卡死都不再让 1s 兜底定时器空转（守住"空闲零定时器"红线）
- [x] 弹出窗打开性能：首屏分片渲染 + 操作按钮懒创建 + 设置读取不挡数据查询
- **验收**：几乎不再感知到 Chrome 原生下载功能的存在；长时间运行内存稳定

---

## 7. 交付方式

Chrome → chrome://extensions → 开启"开发者模式" → "加载已解压的扩展程序" → 选择本项目目录。
